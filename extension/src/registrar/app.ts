import { Server } from ".";

try {
	new Server().start();
} catch (e) {
	require("C:\\Users\\henni\\AppData\\Local\\Yarn\\Data\\global\\node_modules\\easy-attach\\")();
	console.log("error: " + e);
	process.exit(1);
}
